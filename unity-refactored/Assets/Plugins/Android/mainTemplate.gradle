// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

allprojects {
    buildscript {
        repositories {**ARTIFACTORYREPOSITORY**
            google()
            mavenCentral()
            maven { url "https://maven.google.com" }
            maven { url "https://jitpack.io" }
        }
        dependencies {
            // If you are changing the Android Gradle Plugin version, make sure it is compatible with the Android Gradle Plugin.
            // You can refer to the Unity Gradle Plugin release notes to see which Android Gradle Plugin versions are supported.
            // https://docs.unity3d.com/Manual/android-gradle-plugin.html
            classpath 'com.android.tools.build:gradle:7.4.2'
            **BUILD_SCRIPT_DEPS**}
    }

    repositories {**ARTIFACTORYREPOSITORY**
        google()
        mavenCentral()
        maven { url "https://maven.google.com" }
        maven { url "https://jitpack.io" }
        flatDir {
            dirs "${project(':unityLibrary').projectDir}/libs"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

// Android App Bundle configuration
android {
    bundle {
        language {
            enableSplit = true
        }
        density {
            enableSplit = true
        }
        abi {
            enableSplit = true
        }
    }
    
    packagingOptions {
        pickFirst '**/libc++_shared.so'
        pickFirst '**/libjsc.so'
    }
}